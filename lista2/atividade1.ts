let numeros: number[] = [10,12,15,20,22];
let somar: number = 3;

for (let index = 0; index < numeros.length; index++) {
    console.log(`O número ${numeros[index]}, somado com ${somar}, é ${numeros[index] + somar}`);
}